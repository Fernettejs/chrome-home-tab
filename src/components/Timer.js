const Timer = () => {
  return (
    <div
      style={{
        position: "absolute",
        marginTop: "10px",
        padding: "16px",
        backgroundColor: "#102",
        width: "180px",
        borderRadius: "10px",
        color: "#fff",
        textAlign: "right",
      }}
    > Hello
    </div>
  );
};

export default Timer;
